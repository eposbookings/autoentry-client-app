APP_PORT=8000
# For VPS testing by IP, use http://your-server-ip for both values below.
REACT_APP_BACKEND_URL=http://localhost:8000
CORS_ORIGINS=http://localhost:3000
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:8000

DB_NAME=autoentry_portal
DATABASE_URL=mysql+asyncmy://autoentry_user:change-this-db-password@mysql:3306/autoentry_portal
MYSQL_DATABASE=autoentry_portal
MYSQL_USER=autoentry_user
MYSQL_PASSWORD=change-this-db-password
MYSQL_ROOT_PASSWORD=change-this-root-password

JWT_SECRET=replace-with-a-long-random-secret
PAYROLL_INTEGRATION_SECRET=replace-with-an-independent-random-secret-of-at-least-32-characters
# Generate with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=replace-with-a-generated-fernet-key
COOKIE_SECURE=false

OPENAI_API_KEY=
OPENAI_INVOICE_CHECK_MODEL=gpt-5.6-luna

# QuickBooks OAuth.
# Local development callback:
# QUICKBOOKS_REDIRECT_URI=http://localhost:8000/api/integrations/quickbooks/callback
# Live callback:
# QUICKBOOKS_REDIRECT_URI=https://eposbookings.net/api/integrations/quickbooks/callback
QUICKBOOKS_CLIENT_ID=
QUICKBOOKS_CLIENT_SECRET=
QUICKBOOKS_ENVIRONMENT=sandbox
QUICKBOOKS_REDIRECT_URI=

# Xero OAuth (can also be configured from Global integrations).
XERO_CLIENT_ID=
XERO_CLIENT_SECRET=
XERO_REDIRECT_URI=

# Sage Business Cloud Accounting OAuth (can also be configured from Global integrations).
SAGE_CLIENT_ID=
SAGE_CLIENT_SECRET=
SAGE_REDIRECT_URI=

ADMIN_EMAIL=admin@eposaccountancy.co.uk
ADMIN_PASSWORD=change-this-password
# The platform administrator creates and manages accountancy practice tenants.
# Use a separate address and a strong password in every deployed environment.
PLATFORM_ADMIN_EMAIL=platform-admin@example.com
PLATFORM_ADMIN_PASSWORD=change-this-platform-password
DEFAULT_PRACTICE_NAME=EPOS Accountancy Practice
