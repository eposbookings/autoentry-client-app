
services:
  mysql:
    image: mysql:8.4
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE:-autoentry_portal}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    volumes:
      - ./mysql/data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -uroot -p$${MYSQL_ROOT_PASSWORD} --silent"]
      interval: 5s
      timeout: 5s
      retries: 20
  nginx:
    restart: always
    image: nginx:stable-alpine
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - api
      - frontend
    links:
      - "api:api"
      - "frontend:frontend"
    volumes:
      - /var/www:/var/www:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - ./Client App/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
  api:
    restart: always
    image: autoentry-client-app-api:latest
    build:
      context: ./Client App/backend
      dockerfile: Dockerfile
      args:
        JWT_SECRET: ${JWT_SECRET}
        FERNET_KEY: ${FERNET_KEY}
        APP_PORT: ${APP_PORT}
    env_file:
      - ./.env
    environment:
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:3000,http://45.8.225.73,http://eposbookings.net,http://www.eposbookings.net,https://eposbookings.net,https://www.eposbookings.net}
      FRONTEND_URL: ${FRONTEND_URL:-https://eposbookings.net}
      BACKEND_URL: ${BACKEND_URL:-https://eposbookings.net}
      PAYROLL_WORKER_URL: http://payroll-worker:3102
      PAYROLL_INTEGRATION_SECRET: ${PAYROLL_INTEGRATION_SECRET}
    ports:
      - "${APP_PORT:-8000}:8000"
    depends_on:
      mysql:
        condition: service_healthy
      payroll-worker:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health', timeout=3).read()\""]
      interval: 5s
      timeout: 5s
      retries: 24
      start_period: 20s
    volumes:
      - ./Client App/backend/uploads:/app/uploads
  payroll-worker:
    restart: always
    image: autoentry-client-app-payroll-worker:latest
    build:
      context: ./Payroll 2
      dockerfile: Dockerfile.epos
    environment:
      PAYFLOW_NODE_PREVIEW: "1"
      PAYFLOW_LOCAL_DB: /data/payroll.sqlite
      PAYROLL_INTEGRATION_SECRET: ${PAYROLL_INTEGRATION_SECRET}
      PORT: "3102"
    expose:
      - "3102"
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://localhost:3102').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))\""]
      interval: 5s
      timeout: 5s
      retries: 24
      start_period: 30s
    volumes:
      - ./Payroll 2/data:/data
  frontend:
    restart: always
    image: nginx:stable-alpine
    ports:
      - "3000:80"
    volumes:
      - ./Client App/frontend/build:/usr/share/nginx/html:ro
      - ./Client App/frontend/nginx.conf:/etc/nginx/conf.d/default.conf:ro
